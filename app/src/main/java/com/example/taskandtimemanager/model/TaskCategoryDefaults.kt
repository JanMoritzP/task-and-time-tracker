package com.example.taskandtimemanager.model

/**
 * Central place for task category defaults.
 * Category is intentionally hard-coded to LEGACY and not exposed in the UI.
 */
object TaskCategoryDefaults {
    const val DEFAULT_TASK_CATEGORY: String = "LEGACY"
}
